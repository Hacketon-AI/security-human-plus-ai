"""Asset ownership verification (DNS TXT).

Proves an organization controls an asset's hostname before the asset moves from
``pending_verification`` to ``verified``. This stage supports DNS TXT for web
and API assets only.
"""
